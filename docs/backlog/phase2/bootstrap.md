# Shona API Phase 2 Bootstrap

## Pre-flight

## PF-001 - Confirm repository and source of truth
Goal: Use existing repository `rtjhussein/shona-api` and keep GitHub Issues plus a new GitHub Project as the execution tracker.

## PF-002 - Preserve existing completed project
Goal: Treat `Shona API Implementation` as complete and do not reopen its 46 done items.

## PF-003 - Set up Phase 2 project board
Goal: Create `Shona API Phase 2 - Data Growth + Morphology Validation` with the first five execution issues.

## PF-004 - Commit planning documents
Goal: Commit this Phase 2 backlog package before implementation begins.

## Assumptions

- The continuous Hannan ingestion workflow already exists and stays outside this project.
- Phase 2 turns ingestion output into public value through review, publication, morphology validation, search quality, release safety, and figurative seed coverage.
- TDD remains expected for agent-executable implementation work.
- Issues are optimized for one-agent-per-issue execution where practical.
